

# Add Twitter/X Link to Footer

Add a Twitter/X social link (`https://x.com/solarizer_io`) to the footer's brand block, below the email address.

## Changes

**File: `src/components/Footer.tsx`**
- Import the `Twitter` (or `X`) icon from `lucide-react`
- Add a clickable icon link to `https://x.com/solarizer_io` in the brand section, styled consistently with the existing email link

